# Suggestions de code (actions rapides)

- `keylogger.py`: ajouter une option `--duration` pour arret auto.
- `stegano.py`: ajouter un mode decode (lecture message).
- `exif_reader.py`: afficher un message "aucun GPS" si vide.
- `server.py`/`client.py`: ajouter un message de fin de session.
