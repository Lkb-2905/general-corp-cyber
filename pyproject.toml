[project]
name = "general-corp-cyber"
version = "0.1.0"
description = "Outils cyber educatifs et demos."
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }

[project.optional-dependencies]
dev = ["pytest"]

[tool.pytest.ini_options]
testpaths = ["tests"]
